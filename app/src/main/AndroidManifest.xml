<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FINAL_INTENT1">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">

            <!---<activity android:name=".Saudavel" // AQUI deu errado
                android:exported="true"
                android:windowSoftInputMode="adjustResize">
                 />    !-->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <!-- essa é do main para PROCESSAR !-->
                <action android:name="android.intent.action.SEND"/>
            </intent-filter>

            <intent-filter>
                <!-- essa é do PROCESSAR para VER/ABRIR uma activity.xml !-->
                <action android:name = "android.intent.action.VIEW"/>
            </intent-filter>


        </activity>

        <activity
            android:name=".Processar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="decimal/float" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Saudavel"
            android:exported="true">
        </activity>

        <activity
            android:name=".Abaixo"
            android:exported="true">
        </activity>

        <activity
            android:name=".Acima"
            android:exported="true">
        </activity>


    </application>

</manifest>